# 安全与隐私

## 支持范围

安全修复以本仓库默认分支的最新版本为准。这个项目面向单人、可信设备上的本地查看，不是多用户服务，也不提供公网部署方案。

## 安全模型

服务端采用多层只读限制：

- 只绑定 `127.0.0.1`；
- SQLite 连接 URI 使用 `mode=ro`；
- 每个连接启用 `PRAGMA query_only=ON`；
- API 只实现 `GET`，其他 HTTP 方法返回 `405 Method Not Allowed`；
- 页面设置限制性 Content Security Policy，网络连接仅允许同源；
- 前端资源随仓库提供，不依赖 CDN、遥测或外部分析服务。

这些限制防止看板意外写回数据库或直接暴露到网络，但它们不是完整的访问控制：

- `127.0.0.1` 不等于登录认证。同一台设备上的其他用户或进程可能访问正在运行的服务。
- HTTP 没有 TLS。不要通过端口转发、反向代理、容器端口映射或隧道把它暴露到局域网或公网。
- 浏览器会显示题干、答案、错误笔记和作答记录。共享屏幕、浏览器历史、开发者工具和本机日志都可能泄露这些内容。
- 只读打开不能替代文件权限。私人数据库本身应放在仓库之外，并使用操作系统权限保护。
- 本项目假定数据库来自可信本地来源；不要打开来源不明的 SQLite 文件。

使用结束后按 `Ctrl+C` 停止服务。若电脑由多人共用，请使用独立系统账户，并避免让服务长期运行。

## 绝不能提交的内容

不要提交或附在 issue、Pull Request、测试日志及截图中的内容包括：

- 真实 SQLite 数据库及其 `-wal`、`-shm`、journal 或备份文件；
- 真实题目、答案、错误笔记、分数、置信度、作答日期或复习日志；
- 可关联到个人的稳定 ID、导出文件、映射表和同步配置；
- API 密钥、访问令牌、Cookie、OAuth 凭据或本机路径；
- 受版权保护的教材、真题扫描件、PDF、图片、XMind 或课程资料。

`scripts/create_demo_database.py` 在本地生成的 `data/demo.sqlite3` 必须始终保持完全合成，且数据库文件本身被 `.gitignore` 排除。真实数据不能通过“改名为 demo”进入版本控制；仅删除姓名、邮箱或少数文本字段也不足以完成匿名化。

## GitHub Pages 与在线部署

本仓库不提供 GitHub Pages 版本，也不会上传数据库供在线查询。静态前端依赖本地只读 API，单独发布静态文件既不构成可用看板，也不应通过额外服务接入私人数据库。

## 报告漏洞

请优先使用 GitHub 的私密漏洞报告功能，并提供最小化复现步骤、受影响版本和预期影响。不要在报告中附真实数据库或个人学习数据；请使用合成样例复现。

如果仓库未启用私密漏洞报告，可以先提交不含敏感细节的 issue，请维护者建立私密沟通渠道。对于已经公开泄露的凭据或个人数据，应先撤销凭据、停止服务并清理暴露面，不要等待代码修复。
